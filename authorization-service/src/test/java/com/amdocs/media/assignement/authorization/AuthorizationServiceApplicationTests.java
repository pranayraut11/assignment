package com.amdocs.media.assignement.authorization;

/*
 * @SpringBootTest
 * 
 * @Ignore class AuthorizationServiceApplicationTests {
 * 
 * @Autowired private ProfileService profileService = new ProfileServiceImpl();
 * 
 * @Test public void updateProfile() throws NotFoundException,
 * JsonProcessingException { ProfileDTO profile = new ProfileDTO();
 * profile.setMobile("87665463333"); profile.setAddress("A-1");
 * ResponseEntity<Void> response = new ResponseEntity<>(HttpStatus.CREATED);
 * profileService.update(profile); }
 * 
 * }
 */